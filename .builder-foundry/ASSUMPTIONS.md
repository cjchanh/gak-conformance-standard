# Assumptions

| ID | Assumption | Why needed | Evidence | Reversible | Risk | Status |
|---|---|---|---|---|---|---|
| A-001 | Profile `standard` is the least-wrong Foundry profile for a standard+harness (no visual quota). | Mission already chose it; library-cli would lock packaging-as-identity. | `.builder-foundry/MISSION.md`, `contract.json` | yes | low | accepted |
| A-002 | v0 ships the **frozen v1 13-clause** set. v1.1's optional `GAK-AUDIT-CONTENT-BLIND` is capability-gated and not required for first-run. | Mission: "v0 ships the frozen v1 clause set". | `v1/spec.md` §4 + §9; MISSION.md | yes | low | accepted |
| A-003 | Deponent extras `GAK-REDIRECT-DENIED` and `GAK-NEWLINE-CHAINED` are kernel-local probes, **not** GAK v0 clauses. | Live Deponent harness has 16 clauses; spec has 14 IDs. Importing Deponent as harness would expand the category. | `.builder-foundry/evidence/clause-census-drift.json` | yes | high if reversed | accepted |
| A-004 | Digest bytes follow spec §5.3: `json.dumps(obj, sort_keys=True)` **default separators** (spaces after `:` and `,`). Compact JSON is forbidden for digest. | Compact separators produce `65f40400…`, not published `de6b7089…`. | `.builder-foundry/evidence/digest-separator-experiment.json` | no (would fork the standard) | critical if wrong | accepted |
| A-005 | `--adapter module:Class` is the v0 plugin surface. No pickle, no plugin directory scan, no remote install. | Mission example command; importlib residual risk is documented not hidden. | MISSION.md; PEP 621 / import system | yes | medium (import-time exec) | accepted |
| A-006 | Package layout is top-level `gak_conformance/` (not `src/`) so `python3 -m gak_conformance` works from a clone with `PYTHONPATH=.` or repo root on sys.path. | Third-party first success without a network install. | No `pyproject.toml` exists yet; reversible to src-layout later | yes | low | open |
| A-007 | Stdlib-only runtime. No click, pydantic, network, paid APIs. | Research prototype; Apache-2.0 leave-behind. | grok-fleet research 2026-08-12 | yes | low | accepted |
| A-008 | Deponent and sworn siblings are **read-only**. An optional adapter may import them when present; the harness never requires them. | Writable root is this repo only. Campaign env cannot import either today. | baseline-runtime.json | yes | medium (cannot live-score Deponent in this env until optional path exists) | accepted |
| A-009 | Fixture adapters are honest about being **harness fixtures**, not claimed kernels. They exist so a stranger can run a score without Deponent. Mark language must not call a fixture "GAK-conformant" in README marketing. | Spec §6.3 / §8 void fabricated adapters. Fixtures test the *harness*, not a kernel. | spec §6.3, §8 | yes | high if marketed as a kernel score | accepted |
| A-010 | `scripts/check_consistency.py` will be rewritten to score spec ↔ **in-repo** clause table + published evidence, not `from deponent.conformance import CLAUSES`. | Current checker exits 3 without Deponent; also would fail-census if Deponent extras stay. | check-consistency-stderr.txt; clause-census-drift.json | yes | medium | open |
| A-011 | Default CLI harness identifier is `gak-conformance/v1`. `--harness gak-conformance/v1.1` is later optional. | Mission v0 frozen v1. | MISSION.md | yes | low | accepted |
| A-012 | Campaign contract is immutable. No quota reduction. No second marathon start. | `contract.lock.json` matches; pid 22385 treated running. | contract.lock.json; foundry-campaign status | no | dual-start if ignored | accepted |
